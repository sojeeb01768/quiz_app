import React from 'react';




const Chart = ({ data }) => {
    const { name, total } = data
    console.log(data);


    return (
        <div>
         

        </div>
    );
};

export default Chart;